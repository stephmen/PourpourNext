import React from 'react';
import { StyledHeader } from './Header.styled.js';

export default function Header() {

    return(
        <StyledHeader>
        <header>
        </header>
        </StyledHeader>
    )
}